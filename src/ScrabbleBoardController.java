import java.awt.event.ActionListener;
import java.awt.event.ActionEvent;

/*
    This class should be used to place model based methods related to the scrabble game.
    ie:

    - X Words Aligned
    - Y Words Aligned
    - Valid Word
    - Calculate Scores
    - Revert user selection

    etc, etc
 */

public class ScrabbleBoardController implements ActionListener {

    @Override
    public void actionPerformed(ActionEvent e) {
//        System.out.println(e.getActionCommand());
//        String[] clickedLocation = e.getActionCommand().split(",");
//        int[] l = new int[2];
//        System.out.println(e.getActionCommand());
//        l[0] = Integer.parseInt(clickedLocation[0]);
//        l[1] = Integer.parseInt(clickedLocation[1]);
//        coords.add(l);
    }
}
